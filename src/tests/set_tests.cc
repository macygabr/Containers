#include <set>

#include "../s21_set/s21_set.h"
#include "gtest/gtest.h"

TEST(TestGroupName_Set, Set_int_1) {
  // s21::set<int> a;
  // int a1 = 1, a2 = 6, a3 = 3, a4 = 9;
  // a.insert(a1);
  // a.insert(a2);
  // a.insert(a3);
  // a.insert(a4);
  // std::set<int> b;
  // int b1 = 1, b2 = 6, b3 = 3, b4 = 9, b5 = 0;
  // b.insert(b1);
  // b.insert(b2);
  // b.insert(b3);
  // b.insert(b4);
  // b.insert(b5);

  // std::cout << *b.find(2) << std::endl;
  // for (int i = 0; i < 4; i++) ASSERT_EQ(*a.find(i), *b.find(i));
  // a.printTree(a.root);
}
